version: 0.2

env:
  variables:
    EFS_MOUNT: "/mnt/efs"

phases:
  install:
    commands:
      - yum install -y python38 amazon-efs-utils rsync
  pre_build:
    commands:
      - mkdir -p $EFS_MOUNT
  build:
    commands:
      - rsync -av --delete ./ $EFS_MOUNT/
